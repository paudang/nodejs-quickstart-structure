variables:
  NODE_ENV: 'test'

stages:
  - lint
  - test
  - build

cache:
  paths:
    - node_modules/

install_dependencies:
  stage: .pre
  image: node:22-alpine
  script:
    - npm ci

lint_code:
  stage: lint
  image: node:22-alpine
  script:
    - npm run lint

run_tests:
  stage: test
  image: node:22-alpine
  script:
    - npm run test:coverage

build_app:
  stage: build
  image: node:22-alpine
  script:
    - npm run build --if-present
